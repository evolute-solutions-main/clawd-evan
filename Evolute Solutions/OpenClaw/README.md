# OpenClaw — Data Inputs

This folder is for importing and organizing data that should feed into OpenClaw.

## Priority 1: Legacy Dan Co exercises
- Collect the old Dan Co exercises (files, docs, notes, prompts, outputs).
- Normalize/convert into consistent formats (Markdown preferred).
- Add metadata (date, source, topic, intended outcome).

## Goal
Make these assets easily searchable and usable inside OpenClaw so they can inform coaching, decision-making, and Max’s evolution workflow.
